rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Qualquer usuário autenticado pode ler e escrever (app interno)
    match /{allPaths=**} {
      allow read: if request.auth != null;
      allow write: if request.auth != null;
    }
  }
}
